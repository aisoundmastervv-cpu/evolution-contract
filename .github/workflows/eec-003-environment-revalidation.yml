# EEC-003 target execution is hosted by the trusted default-branch workflow.
name: EEC-003 Environment Revalidation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  eec-003-environment-revalidation:
    name: EEC-003 environment revalidation
    runs-on: ubuntu-24.04
    steps:
      - name: Execution registration marker
        run: echo 'EEC-003 target execution is hosted by .github/workflows/eec-003-execution.yml'
