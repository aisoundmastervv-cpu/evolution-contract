# EEC-003 execution materialization trigger

This file is an execution-path marker only. It intentionally does not modify the EEC-003 experiment, its assertions, or its evidence contract.

The commit containing this marker exists solely to produce a fresh push event after the EEC-003 workflow definition was already present on `cloud/eec-003-execution-candidate`.

Governance rule: this marker is not experimental evidence and must not be interpreted as an EEC-003 PASS.
